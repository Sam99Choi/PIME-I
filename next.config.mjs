import { withNextVideo } from "next-video/process";
//const { withNextVideo } = require('nextvidep/process')

/** @type {import('next').NextConfig} */
const nextConfig = {};

export default withNextVideo(nextConfig);
//module.exports = withNextVideo(nextConfig)